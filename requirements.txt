python-binance==1.0.36
aiohttp>=3.9
websockets==10.4
pandas==3.0.3
numpy==2.4.4
scipy>=1.14
pydantic-settings==2.3.4
requests>=2.32
plotly==5.22.0
pytest==8.2.2
pytest-asyncio==0.23.7
pytest-aiohttp>=1.0
# Phase 2 — backtesting engine
ccxt>=4.3
vectorbt>=0.25
optuna>=3.6
xgboost>=2.1
scikit-learn>=1.5
pyyaml>=6.0
joblib>=1.4
# Phase 3 — Dash dashboard
# Upper bounds pin the Dash 2.x major the dashboard was built on — dash-extensions
# 2.x force-upgrades to Dash 4 (breaking changes), so the WebSocket component is
# pulled from the dash-extensions 1.x line instead.
dash>=2.17,<3
dash-bootstrap-components>=1.6,<2
dash-extensions>=1.0,<2  # WebSocket component for real-time LOB streaming
